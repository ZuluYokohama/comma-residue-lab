# Failed Hypothesis H0

**Statement**: The floating-point evaluation of λ(12·e_fifth - 7·e_oct) is numerically zero (abs < 1e-15).

**Status**: Rejected after MEASURE (see measurements/H0_result.json).

**Residue preserved**: the measured log residual and its cents equivalent remain in the repository. No post-hoc adjustment of generators was performed to force the residual to zero.
