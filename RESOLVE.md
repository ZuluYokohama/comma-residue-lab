# RESOLVE

## Strategy

The measured residual R = λ(12·e_fifth - 7·e_oct) is non-zero.

Two admissible responses under the RESTRICT invariants:

A. Keep the pure generators and accept an explicit obstruction (the comma remains a first-class residual object).
B. Construct a new generator whose twelfth power lands exactly on seven octaves, i.e. absorb R uniformly.

Both are recorded. B does not delete the pure residual.

## Construction of tempered fifth (response B)

Required: 12 * log(fifth_t) = 7 * log(2)

Therefore log(fifth_t) = (7/12) * log(2)

fifth_t = 2^(7/12)

The pure residual is left untouched in residue/.
The tempered system is a separate computational object.
